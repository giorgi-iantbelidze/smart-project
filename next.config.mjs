/** @type {import('next').NextConfig} */
const nextConfig = {
     images:{
        domains: ["fakestoreapi.com", "cdn.prod.website-files.com", "www.startpage.com",]
    }

};

export default nextConfig;
